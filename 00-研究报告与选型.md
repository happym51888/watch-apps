# Apple Watch 未被满足的需求：调研、筛选与最终三选

调研日期：2026-09-03
方法：四路并行检索，约 64 次搜索、21 个完整抽取的论坛主题、172 条引用链接分布在 52 个域名，另加 GitHub Issues、V2EX、Apple 开发者论坛与 Apple 官方文档做可行性核验。

原始报告存放位置（三份，均可独立查阅）：

- `C:/Register/code/workspace/dialogues/20260903-1827-watchos-unmet-app-demand/REPORT.md`
- `C:/Register/code/workspace/dialogues/20260903-1827-watchos-supply-gap/watchos-supply-side-report.md`
- `C:/Register/code/workspace/dialogues/20260903-1828-applewatch-unmet-needs/apple-watch-unmet-needs-report.md`

---

## 一、先说结论：题目要改一个字

你要找"尚未被实现的需求"。在一个已经十年的平台上，**需求量大且完全没人做过的东西，基本不存在**。真正存在的是另一类东西，而且它更值钱：

> **需求已被验证，供给存在但坏掉了。**

这类机会比"空白市场"好，因为需求不需要你去教育——用户已经在付费、已经在抱怨、已经在自己手工绕过。

而 watchOS 上这类机会异常多，原因不在需求侧，在平台侧。三条硬事实：

1. **2026 年 4 月 28 日起**，任何 watchOS 应用上传都必须用 watchOS 26 SDK 构建并支持 64 位。老应用还能运行，但开发者**改一张截图都得先整包重建**。很多人不会回来重建。
2. **watchOS 26 直接打断了现有应用。** 某个主流间歇计时器的更新日志原话是"没有办法修，我只能把手表端整个重写"，并承认丢了功能。Apple 甚至在 watchOS 27 之前自己重新处理了部分第三方二进制文件，因为有些应用打不开。
3. **watch-only 应用完全没有 App Store Analytics** ——没有获取、留存、崩溃、归因数据，只有销售报表。上架还需要一个没有文档的 `ITSWatchOnlyContainer` 壳，因为 Xcode 根本没给独立手表应用提供上架通道。

第 3 点是成本，前两点是机会。**现在正好是一个窗口期：一批有真实用户的应用正在因为重建成本而腐烂。**

---

## 二、被我否掉的方向（这部分比推荐更有价值）

如果只看需求信号，下面这些全都会入选。它们没入选，理由分三类。

### 2.1 Apple 已经自己做了

已被第一方吸收：**用药提醒、经期记录、呼吸练习、语音备忘录、账单分摊**。

顺带纠正一个流传很广的说法：常有人说 Apple Watch 没有语音备忘录，**这是错的**，watchOS 6 起就有。

心率区间提醒同理，现在是原生功能。

### 2.2 最近一两年已经被填满

**宫缩计时器**（价格已被打到 0）、**桑拿/冷泉计时**、**演讲计时**、**编织行数计数器**（2025–26 一年内上了五个以上：StitchLog、Woolly Counter、Knittle、Ply、My Row Counter、YarnBuddy）、**静音触觉计时器**（Shush、AtLeast、Tap Timer）、**姿势提醒**、**习惯打卡**。

**经期倒数复杂功能**是个典型的"伪空白"：有用户明确说"我找不到能做这个的"，但 Flo 和 Cycles 都已经有了。这是发现问题，不是供给问题。

**步数数字复杂功能**也一样——三个独立主题在抱怨，但 Pedometer++ 早就做了。

### 2.3 平台根本不允许

| 方向 | 呼声 | 为什么做不了 |
|---|---|---|
| 第三方表盘 | 平台最大抱怨，某主题下 23 条追评 | 无 API。现有"表盘应用"都是壁纸绕过，有用户直接说"感觉都很可疑" |
| 智能闹钟（按睡眠阶段唤醒） | 有用户为此换 Garmin | 实时睡眠分期不对第三方开放，只能靠体动瞎猜，而这正是用户拒绝的 |
| WhatsApp / Telegram 原生手表版 | 语料中出现频率最高的单项请求 | 只有 Meta 能做 |
| 真正的对讲（Walkie-Talkie 替代） | watchOS 27 移除了官方对讲，9to5Mac 直说"会有第三方来填" | **PushToTalk 框架只有 iOS 版**。可以做异步语音，做不了常驻监听 |
| 血压 | 直接需求 | 无传感器权限，且属受监管医疗器械 |
| 离线 Spotify / Audible / Libby | 长期抱怨 | DRM 墙，不是代码问题 |
| 自定义通知震动 | 多次出现 | watchOS 无 CoreHaptics，只有 9 种固定 `WKHapticType` |

### 2.4 一个必须记住的审核陷阱

Apple DTS 工程师在开发者论坛明确表态：在非运动类应用里用一个假的 workout session 来换取后台运行时间，**"有被 App Review 拒绝的风险"**，并说持续后台执行"从第一天起就不是被支持的用例"。

这一条直接砍掉了大部分"被动传感"类点子。后台 HealthKit 投递现实速率是每小时 1–4 次，不是实时。

---

## 三、最终三选

选择标准按顺序：需求可量化 > 供给确实坏了 > 平台允许 > **我能在没有 Mac 的情况下证明核心逻辑是对的**。

最后一条是这次的硬约束，也直接影响了排序——见第四节。

### 第一名：Kairos — 手表原生 TOTP 双因素验证器

**证据是三个里最硬的，因为它可以计数。**

两个仍然开着的功能请求：

> it would be nice to have an apple watch app for authenticator so people can find their auth codes without pulling out their phone

78 个 👍，2017 年提出，维护者一次都没回复。另一个验证器上的平行请求是 33 个 👍。

现有产品的弱点被用户描述得异常精确：OTP Auth 的手表端需要**配对的 iPhone 处于解锁状态**，用户原话——"which defeats the purpose of using the watch"。如果为了看手表上的验证码得先解锁手机，那手表的意义已经没了。

**产品命题就一句话：录入一次，之后永远不需要手机。** 手机可以关机、没电、锁在健身房柜子里、在另一个国家。

这也命中了整个调研中出现频率最高的主题——九个独立来源都在表达"想把手机留下"。

### 第二名：Tactus — 触觉节拍器

需求由一个**硬件市场**验证：Soundbrenner 卖 70–130 美元的可穿戴节拍器，官方 FAQ 里专门有一条"为什么我不能直接用 Apple Watch"。有人愿意为这个功能单独买一块设备。

而软件端是坏的。现有主流应用的差评是同两句话的循环："haptic is too faint"、"Doesn't work on Apple Watch"、"stuck on an endless loop of loading"。

**技术差异点是真实的。** Apple 规定两次触觉调用之间最少 100ms，且新调用会取消正在进行的那次。超过约 176 bpm，逐拍触觉在物理上就无法干净送达——这正是那些应用"糊掉"的原因。Tactus 主动做减法（先丢附点、再丢弱拍），并在屏幕上说明它丢了什么。

**需要如实说明的反面信号：** 调研期间发现有一个 r/percussion 用户自己做了一个手表原生、无订阅的节拍器。竞争变强了，但在一个硬件卖 70–130 美元的市场里，多一个独立开发者并不等于机会关闭。

### 第三名：Awqat — 礼拜时间 + 朝向 + 计数器

受众规模巨大，且评价最好的手表应用坏了：

> hands down the BEST salah times app for the watch. But recently it has not been functioning at all… the widget appears as just a blank square.

**但调研让我把这个应用的卖点整个换掉了。** 原本的想法是"礼拜时间算得准"——这个赛道确实拥挤，光算得准不构成理由。真正的缺口是另一件事，一位开发者说得很清楚：

> every prayer app delivers the pre-dawn (Fajr / suhoor) alarm as a notification. iOS caps notification sound at 30 seconds

30 秒的提示音叫不醒真正睡着的人。所以这类应用的抱怨从来不是"时间算错了"，而是**错过晨礼**。

watchOS 有专门的机制：`.alarm` 类型的扩展运行时会话，提前调度、到点唤醒应用、在手腕上反复震动，静音模式下照样有效。限制很严（最多提前 36 小时、同时只能有一个、启动时必须播放触觉），但它是对的工具。**通知是提醒，闹钟才是叫醒。**

---

## 四、为什么排序是这样：一个诚实的约束

你没有 Mac，我也无法在 Windows 上编译 Swift（工具链装好了，但标准库需要 Windows SDK，而 MSVC 构建工具的安装需要管理员权限，装失败了）。SwiftUI 和 WatchKit 更是根本无法在 Apple 平台之外编译。

所以我没有办法交付"编译通过"的保证。我能做的是**把可能算错的逻辑单独拆出来，移植成 Python 跑起来，用外部公开数据对答案**。

这直接影响了排序：

| 应用 | 可对照的外部权威数据 | 断言数 | 结果 |
|---|---|---|---|
| Kairos | RFC 4226 附录 D、RFC 6238 附录 B、RFC 4648 §10 | 47,954 | 全部通过 |
| Tactus | 无外部基准，改用性质穷举 | 61,233 | 全部通过 |
| Awqat | AlAdhan API 的 36 个已发布时刻 | — | 30 个完全一致，6 个差 60 秒内 |

Kairos 排第一，很大程度上是因为 **TOTP 有官方测试向量**。这意味着在没有 Mac 的前提下，它的核心正确性是三者中唯一可以被第三方标准逐字节验证的。对一个验证器来说这尤其重要：算错了不会报错，只会被网站拒绝，然后用户以为是自己输错了。

三个具体的、只要写错就会间歇性失败的点，都已钉死在测试里：

- **前导零**。RFC 表里放 `07081804` 就是为了这个。当成整数格式化，十次登录挂一次。
- **符号位掩码**。RFC 4226 §5.3 要求屏蔽首字节最高位。漏掉的话，只在该字节 ≥ 0x80 时出错。
- **每种哈希的种子长度不同**。RFC 6238 的 SHA-256/512 行用的是 32 和 64 字节种子。三种都用 20 字节种子，是"看起来通过了表格但其实是错的"的标准姿势。

Awqat 那 6 个 60 秒内的差异，我没有直接接受。反算两边时间各自隐含的影长系数：**AlAdhan 是 0.98–0.99，而它自己声明的是 1.0；本引擎是 1.0000**。分歧在对方。这个结论写进文档而不是"修掉"，因为要对齐就必须故意算错 Asr。

顺带一提，Python 移植也暴露了它自己的边界：Python 整数是任意精度，`10**10` 算得很开心，而 Swift 的 `UInt32` 在同一行会直接 trap。这个 bug 是写 Swift 测试时发现的，不是移植跑出来的——现在两边都钉住了。

---

## 五、调研覆盖不到的地方

必须说明，否则上面的结论会被高估：

- **X/Twitter、YouTube 评论、Quora、Discord、知乎、小红书、B 站全部无法从当前环境访问**，报告里没有任何结论来自这些平台。承担权重的是 GitHub Issues 和 V2EX，因为它们会暴露真实的 reaction 数和浏览数。
- **Reddit 直连被拒（403）**，内容经第三方检索工具获取；**每条评论的具体票数拿不到**，报告里凡是声称"呼声高"的地方，用的是 Reddit 自己渲染的"N more replies"标记，其余关于规模的说法都标了未验证。
- **Product Hunt 拒绝直接抓取**，相关数字是二手的，已标注。
- **无法核实任何可信的 watchOS 应用总数**——流传的"10,000+"追溯到一个没有出处的 SEO 页面。
- 两个评论聚合站的页面里**嵌入了指示 AI 引用它们的文字**，相关数量级已按不可信处理。

---

## 六、一个结构性风险，建议在动手前先接受它

手表端的应用发现机制是坏的，用户自己在说：

> The Apple Watch App Store is a joke. Same old Apps, rarely updated. […] Can't even browse by a category.

下面跟着两条："我都忘了还有个手表应用商店"、"用了五年 Apple Watch，这个商店怎么进？"

一位开发者的反面意见更直接：**"不是应用不好，是这类应用卖不动。"**

两个推论：

1. **优先选择 iPhone 应用本身就是店面的需求。** Kairos 属于这一类（它是带手表端的正常 iOS 应用，不需要 watch-only 那套壳，也拿得到完整的 Analytics）。Tactus 和 Awqat 是 watch-only，代价是没有任何分析数据。
2. **优先选择用户本来就会去搜的词。** "authenticator"、"metronome"、"prayer times" 都是现成搜索词，这三个都满足。
